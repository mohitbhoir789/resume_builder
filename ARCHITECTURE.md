# Architecture & Data Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    RESUME BUILDER SYSTEM                         │
└─────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────┐
│  LOCAL MACHINE                                                             │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────┐         ┌──────────────────────────────────────┐ │
│  │  Jupyter Notebook   │         │      Web Application                 │ │
│  │                     │         │                                      │ │
│  │ ingest_profile.ipynb│         │  ┌──────────────────────────────┐   │ │
│  │                     │         │  │  Frontend (Next.js)          │   │ │
│  │ - PDF/URL/Text ────→│         │  │  http://localhost:3000       │   │ │
│  │ - Extract sections  │         │  │                              │   │ │
│  │ - Train embeddings  │         │  │  Profile Name Input          │   │ │
│  │ - Save to cache     │         │  │  Job Details Input           │   │ │
│  └─────────────────────┘         │  └──────────────────────────────┘   │ │
│           │                       │           │                         │ │
│           │                       │           │ HTTP Request            │ │
│           ▼                       │           ▼                         │ │
│  ┌─────────────────────────────┐ │  ┌──────────────────────────────┐   │ │
│  │  profile_cache/             │ │  │  Backend (FastAPI)           │   │ │
│  │                             │ │  │  http://localhost:8000       │   │ │
│  │ my_profile/                 │ │  │                              │   │ │
│  │ ├─ _profile.json            │ │  │  POST /resume/generate ──────┼───┤ │
│  │ ├─ _embeddings.pkl          │ │  │                              │   │ │
│  │ └─ _metadata.json           │ │  │  1. Load Profile (by name)   │   │ │
│  │                             │ │  │  2. Extract Keywords         │   │ │
│  │ senior_dev/                 │ │  │  3. Map to Resume            │   │ │
│  │ ├─ _profile.json            │ │  │  4. Score ATS               │   │ │
│  │ ├─ _embeddings.pkl          │ │  │  5. Generate LaTeX          │   │ │
│  │ └─ _metadata.json           │ │  │  6. Render PDF              │   │ │
│  └─────────────────────────────┘ │  └──────────────────────────────┘   │ │
│                                   │           │                         │ │
│                                   │           ▼                         │ │
│                                   │  ┌──────────────────────────────┐   │ │
│                                   │  │  artifacts/                  │   │ │
│                                   │  │  ├─ resume_1234.pdf          │   │ │
│                                   │  │  └─ resume_5678.pdf          │   │ │
│                                   │  └──────────────────────────────┘   │ │
│                                   │                                      │ │
│                                   └──────────────────────────────────────┘ │
│                                                                             │
└───────────────────────────────────────────────────────────────────────────┘
```

## Data Flow: Profile Ingestion

```
STEP 1: ONE-TIME INGESTION
════════════════════════════

User launches Jupyter notebook:
jupyter notebook ingest_profile.ipynb

    ┌─────────────────────────────────────────────────┐
    │  Choose Input Method:                            │
    │  ┌─────────────┐  ┌─────────────┐  ┌──────────┐│
    │  │ PDF Path    │  │   URL       │  │ Plaintext││
    │  └────┬────────┘  └────┬────────┘  └────┬─────┘│
    └───────┼────────────────┼────────────────┼──────┘
            │                │                │
            ▼                ▼                ▼
    ┌──────────────────────────────────────────────────┐
    │  Load Resume Content                             │
    │  └─→ PDF to text, URL download, plaintext loaded│
    └────────────────┬─────────────────────────────────┘
                     │
                     ▼
    ┌──────────────────────────────────────────────────┐
    │  Extract Sections                                │
    │  ├─ Experience                                   │
    │  ├─ Projects                                     │
    │  ├─ Skills                                       │
    │  ├─ Education                                    │
    │  └─ Certifications                               │
    └────────────────┬─────────────────────────────────┘
                     │
                     ▼
    ┌──────────────────────────────────────────────────┐
    │  Create Chunks                                   │
    │  └─→ Split into semantic chunks                  │
    └────────────────┬─────────────────────────────────┘
                     │
                     ▼
    ┌──────────────────────────────────────────────────┐
    │  Train Embeddings                                │
    │  └─→ Using E5 or Hashing provider                │
    └────────────────┬─────────────────────────────────┘
                     │
                     ▼
    ┌──────────────────────────────────────────────────┐
    │  Save to profile_cache/                          │
    │  ├─ my_profile_profile.json                     │
    │  ├─ my_profile_embeddings.pkl                   │
    │  └─ my_profile_metadata.json                    │
    └──────────────────────────────────────────────────┘

    Result: Profile ready for resume generation!
```

## Data Flow: Resume Generation

```
STEP 2: RESUME GENERATION (Repeat as needed)
═════════════════════════════════════════════

User opens Web UI:
http://localhost:3000

    ┌────────────────────────────────────────┐
    │  Frontend Form:                         │
    │  ├─ Profile Name: "my_profile"        │
    │  ├─ Job Title: "Senior Engineer"      │
    │  ├─ Company: "TechCorp"               │
    │  ├─ Location: "Remote"                │
    │  └─ JD: "We seek a backend engineer..."│
    └────────────────┬───────────────────────┘
                     │
                     │ HTTP POST
                     ▼
    ┌────────────────────────────────────────┐
    │  Backend: POST /resume/generate        │
    │  ┌─────────────────────────────────────┤
    │  │ Load Profile by Name                │
    │  │ └─→ my_profile_profile.json         │
    │  └─────────────────────────────────────┘
    └────────────────┬───────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────┐
    │  Extract Job Keywords                  │
    │  ├─ Technical skills                  │
    │  ├─ Soft skills                       │
    │  └─ Domain knowledge                  │
    └────────────────┬───────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────┐
    │  Map to Resume Content                 │
    │  (Semantic matching using embeddings)  │
    │  ├─ Matched keywords: 25               │
    │  └─ Missing keywords: 5                │
    └────────────────┬───────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────┐
    │  Score ATS Compatibility               │
    │  ├─ Keyword coverage: 83%              │
    │  ├─ Role relevance: 92%                │
    │  └─ Final Score: 87/100                │
    └────────────────┬───────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────┐
    │  Generate LaTeX Resume                 │
    │  ├─ Add matched skills                 │
    │  ├─ Highlight relevant experience      │
    │  └─ Keep 1-page limit                  │
    └────────────────┬───────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────┐
    │  Render to PDF                         │
    │  ├─ Run pdflatex                       │
    │  ├─ Verify 1 page                      │
    │  └─ Save to artifacts/                 │
    └────────────────┬───────────────────────┘
                     │
                     │ HTTP Response
                     ▼
    ┌────────────────────────────────────────┐
    │  Frontend Shows Results:                │
    │  ├─ ATS Score: 87                      │
    │  ├─ Matched Keywords: 25               │
    │  ├─ Missing Keywords: 5                │
    │  ├─ Download PDF Link                  │
    │  └─ View Audit JSON                    │
    └────────────────────────────────────────┘

    User clicks "Download PDF" → Resume saved!
```

## Directory Structure

```
project_root/
│
├── 📓 ingest_profile.ipynb              ← START HERE (one-time)
│   └─ Ingest resume and save profile
│
├── 🌐 start.sh                          ← START HERE (keep running)
│   ├─ Starts frontend (port 3000)
│   └─ Starts backend (port 8000)
│
├── 📁 profile_cache/                    ← Profile Storage
│   ├── my_profile_profile.json
│   ├── my_profile_embeddings.pkl
│   ├── my_profile_metadata.json
│   │
│   ├── senior_dev_profile.json
│   ├── senior_dev_embeddings.pkl
│   └── senior_dev_metadata.json
│
├── 📁 frontend/
│   ├── app/
│   │   └── page.tsx                    ← Profile name input
│   └── components/
│       ├── ResumeGenerate.tsx          ← Send profile_name
│       └── ResultsPanel.tsx
│
├── 📁 backend/
│   ├── app/
│   │   ├── storage/
│   │   │   └── profile_loader.py       ← Load profiles by name
│   │   ├── services/
│   │   │   ├── orchestrator.py         ← Uses ProfileLoader
│   │   │   └── ingest.py
│   │   └── models/
│   │       └── schemas.py              ← profile_name support
│   │
│   └── main.py
│
├── 📁 artifacts/                        ← Generated PDFs
│   └── resume_12345.pdf
│
└── 📄 Documentation
    ├── CHANGES_SUMMARY.md               ← Overview
    ├── PROFILE_MANAGEMENT.md            ← Usage guide
    ├── IMPLEMENTATION_SUMMARY.md        ← Technical details
    └── QUICK_START.sh                   ← Quick reference
```

## User Journey

```
First Time User (One-time setup)
═════════════════════════════════

1. Install & Navigate
   $ cd resume_builder
   $ jupyter notebook ingest_profile.ipynb

2. Ingest Resume
   Choose: PDF path, URL, or plaintext
   └─→ Profile saved as "my_profile"

3. Start App
   $ ./start.sh
   └─→ Open http://localhost:3000

4. Generate First Resume
   Profile Name: my_profile
   Job Title: Software Engineer
   JD: [paste job description]
   └─→ Download optimized PDF

Returning User (Generate new resumes)
═════════════════════════════════════

1. Start App
   $ ./start.sh
   └─→ Open http://localhost:3000

2. Generate Resume
   Profile Name: my_profile    (already trained)
   Job Title: Backend Engineer
   JD: [paste different JD]
   └─→ Download new PDF

Power User (Multiple profiles)
══════════════════════════════

1. Create Multiple Profiles
   $ jupyter notebook ingest_profile.ipynb
   
   First run: Save as "junior_dev"
   Second run: Save as "senior_dev"
   Third run: Save as "data_scientist"

2. Generate for Any Role
   Profile: junior_dev → Junior position PDF
   Profile: senior_dev → Senior position PDF
   Profile: data_scientist → DS position PDF
```

## Technology Stack Visualization

```
┌─────────────────────────────────────────────────────────┐
│                   User's Machine                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Frontend Layer (React/Next.js 16)                    │
│  ├─ Pure client-side rendering                        │
│  ├─ No backend dependencies                           │
│  └─ HTTP calls to /resume/generate                    │
│      ▲                                                 │
│      │ HTTP POST                                       │
│      │                                                 │
│  Backend Layer (FastAPI)                              │
│  ├─ REST API endpoints                                │
│  ├─ Profile loader                                    │
│  ├─ Keyword extraction & mapping                      │
│  ├─ ATS scoring                                       │
│  └─ PDF generation (LaTeX)                            │
│      ▲                                                 │
│      │ Disk read                                       │
│      │                                                 │
│  Storage Layer (Local)                                │
│  ├─ profile_cache/ (profiles & embeddings)           │
│  ├─ artifacts/ (generated PDFs)                       │
│  └─ No external dependencies!                         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## Comparison: Before vs After

```
BEFORE (Profile Ingest in Frontend)
═══════════════════════════════════

User → Upload Resume → Frontend trains embeddings → Slow!
              ↓                    (CPU intensive)
           IngestService         ✗ Progress unclear
              ↓                   ✗ Can timeout
           Store in memory        ✗ Lost on refresh
              ↓
        Generate Resume


AFTER (Profile Pre-ingest via Notebook)
════════════════════════════════════════

User → Jupyter Notebook ─→ Train embeddings (one-time)
            ↓                └─→ Save to profile_cache/
       (takes 1-2 min)              ✓ Fast UI
            │                       ✓ Reusable
            ▼                       ✓ Offline
    profile_cache/  ◄─── Frontend loads by name
            │               ✓ Instant
            ▼               ✓ No training
    Generate Resume        ✓ Deterministic
```

---

This architecture is designed for **simplicity**, **speed**, and **reliability**!
